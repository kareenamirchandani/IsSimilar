{
  "$schema": "http://json-schema.org/draft/2019-09/schema#",
  "title": "Schema for the data passed to the IsSimilar Grading function.",
  "type": "object",
  "properties": {
    "answer": { "type": "number" },
    "response": { "type": "number" },
    "params": {
      "type": "object",
      "properties": {
        "atol": { "type": "number" },
        "rtol": { "type": "number" }
      }
    }
  },
  "required": ["answer", "response"],
  "additionalProperties": false
}
